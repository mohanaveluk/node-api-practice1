

var loggerModel = class LoggerModel {
    static async logInfo(message) {
        console.log(message);
    }

    static async logWarning(message) {
        console.log(message);
    }

    static async logError(message) {
        console.log(message);
    }

}

module.exports = loggerModel;